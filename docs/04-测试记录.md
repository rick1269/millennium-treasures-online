# 测试记录

更新：2026-09-24。命令：`npm test`。**26 个测试通过，0 个失败。** 游客模式与离线托管已部署到 Supabase 和公开网页；离线托管的端到端断线场景仍以自动测试为主。

## 已覆盖的场景

| 场景 | 结果 |
|---|---|
| 全库数量、副本编号与每时代 3/3/2/1 配比 | 通过 |
| 3、4、5、6 人；三类出价/出售策略、三种固定随机序列，共 36 局 | 全部完成，无卡死 |
| 200 个额外随机牌序整局 | 全部完成；每步检查实体牌数量与 ID 唯一 |
| 抽到五张同星、存在异星却选同星 | 例外允许 / 非法选择被拒 |
| 暗拍同价、第一名资金不足、顺位成交、下回合禁拍 | 通过 |
| 终局完整套、重复卡、红卡罚分和负分 | 通过 |
| 终局公开视图的回合数不超过总回合数 | 通过；线上终局显示 9 / 9 |
| 旁观者与普通玩家无法读到主持候选卡及他人私牌 | 通过 |
| 换卡被拒与被同意 | 通过；双方卡数守恒 |
| 二次拍卖流拍 | 卡退还卖家 |
| 非主持人越权选品、主持人越权竞价、非法小数报价 | 服务端拒绝 |
| 房主增减机器人、6 席上限、其他玩家越权、机器人身份冒用 | 合法操作成功，非法操作拒绝 |
| 单人对 2、3、4、5 位机器人；每种人数 40 个随机牌序，共 160 局 | 全部完成；每步检查实体牌守恒与金币非负 |
| 机器人换卡回应、自动竞价、主持和出售 | 自动推进到需要真人决定的阶段 |
| 云端账号席位绑定、无凭证操作、越权操作、旁观信息裁剪 | 无有效账号或游客凭证时不能操作席位；私牌与本地玩家令牌不泄露 |
| 两个并发加入请求 | revision 乐观锁让一个成功、一个返回 409，不覆盖房间 |
| 云端跨域预检 | 允许网页携带账号令牌调用 API |
| Supabase 网关保留或去掉函数路径前缀 | 三种路径均正确识别建房接口 |
| 云端每步经过 JSON 数据库存取，1 真人 + 2 机器人完成整局 | 正常终局，9 回合持续推进 |
| 游客建房、入席、操作与凭证隔离 | 有效凭证仅能操作自己的席位；服务端不保存明文凭证 |
| 游客离线托管、账号离线托管与重新接管 | 游客原凭证失效；账号回来后恢复人工操作 |
| 刷新缓冲、旧页面迟到的关闭通知、无关闭通知的超时兜底 | 刷新不误托管；旧通知不覆盖新连接；超时仍能托管 |
| 游客房主离开等待页 | 在线玩家接任房主，可继续设置机器人并开局 |

## 实际界面和联通检查

- 在浏览器打开首页，检查中文排版、创建房间、三人入席、主持人选品、明拍画面。
- 本机 `GET http://localhost:8787/` 与局域网地址 `GET http://192.168.3.30:8787/` 均返回 200。
- 外网临时 HTTPS 首页 `GET` 返回 200，`POST /api/create` 成功生成房间与玩家令牌。
- 机器人版通过实际 HTTP 接口创建房间、设置 2 位机器人、开局并由真人选品；两场拍卖由机器人自动完成，进入二次交易阶段。当前房间 `547184` 的等待页已验证可调整为 2 位机器人，且外网链接返回 200。
- 82 个 SVG（81 张卡面、1 张卡背）通过 XML 解析检查。
- GitHub Pages 公开网页已能在浏览器显示注册、登录、查看房间入口，使用 HTTPS 且不依赖本机进程。
- 使用 publishable key 直接读取 Supabase `game_rooms` 返回 HTTP 401 / `permission denied for table game_rooms`，确认浏览器不能绕过服务端读取私牌存档。
- Supabase Edge Function `game` 已发布。线上 `OPTIONS /api/create` 返回 204 且含所需 CORS 标头；最初账号专用版本拒绝未登录建房，更新后的版本允许生成游客凭证并建房；不存在的房间返回 404。
- 真实邮箱注册请求已抵达 Supabase Auth。2026-09-24 01:02 使用独立测试账号从公开网页注册时，Auth 日志记录 `POST /signup`、HTTP 500、163 SMTP 返回 `535 authentication failed`。已核对自定义 SMTP 开关、`smtp.163.com:465`、发件地址与 Username 一致。本机以相同发件邮箱和授权码直连 SMTP 登录成功；将该授权码重新保存到 Supabase 后，01:12 再次注册返回 HTTP 200，日志记录 `user confirmation requested`。收件人收到验证邮件并点击链接，随后网页用该账号成功登录。
- 真实云端房间 `BB3C98`：1 位真人 + 2 位机器人完成 9 回合；覆盖选品、机器人明拍/暗拍、真人明拍多次出价并拍得红卡、真人密封报价并拍得藏品、卖出红卡、机器人自动主持、流拍、终局排名与金币结算。终局首次显示“第 10 / 9 回合”，已修正服务端公开视图并重部署；刷新线上房间后显示“第 9 / 9 回合”，排名及存档保持一致。
- 连续点击同一操作会发送重复请求并产生多余错误提示；网页现会在请求进行中阻止重复操作。更新后的 `app.js` 已发布到 GitHub Pages，并通过读取公开脚本确认上线。
- 新增 `game_presence` 表并允许游客房间没有账号房主，云端迁移成功；重新部署 `game` Edge Function。真实云端 API 验证游客建房、设置 2 位机器人、开局和读取房间状态。
- 将卡牌素材 `v1.1.0` 的 81 张卡面、卡背、总览共 83 张图片提交到 GitHub（`a634dd3`），并发布新网页代码（`5fb24af`）。公开页面实测游客建房、添加 2 位机器人、开局、5 张候选卡全部加载为 `v1.1.0` 图片（宽 815 像素）、选出两张卡并进入二次交易阶段；机器人自动完成明拍与暗拍。
- GitHub 已建立并发布 `v1.1.0` 标签与 Release，指向网页代码提交 `5fb24af`，便于回看这一版的静态网页和卡牌图片。

## 仍需真人试玩的项目

自动测试只能证明已编码场景没有出现这些错误，不能证明平衡性或所有体验细节。邮箱注册、验证、登录和机器人整局已通过真实云端测试；仍需两名真人使用不同账号、设备和网络做一次加入同房间的长局测试。建议下一轮组织至少 20 局真人试玩，记录流拍率、红卡成交率、平均回合时长、先手胜率，以及玩家是否理解资金骰和红卡罚分。不同运营商网络上的长局稳定性与手机浏览器兼容性尚未由真实远端设备验证。
