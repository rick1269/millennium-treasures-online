# 项目文档同步规则

本工程的规则、游戏设定或 UI 有任何改动时，同步更新 `docs/06-三人局UI与产品设计确认稿.md` 中受影响的三人场景、步骤和产品确认项，重新生成 `docs/product-spec-assets/` 的真实界面截图与 `output/pdf/拍卖大亨_三人局_UI与产品设计确认稿.pdf`。在交付代码改动前运行 `npm run docs:product`、`npm test` 和 `npm run build:cloud`；如果只改文字或排版，运行 `npm run docs:product:pdf` 与 `npm run docs:check`。新增功能应扩充场景清单，过时描述应同步修正。

PDF 的可编辑源稿是上述 Markdown。截图脚本为 `scripts/capture-product-spec.mjs`，PDF 构建脚本为 `scripts/build-product-spec.py`。先改源稿，再重新生成发布物。

# Codex 提交与线上发布硬性要求

每次 Codex 准备提交本仓库改动（包括仅修改文档）时，都按 `docs/07-版本发布操作要求.md` 完成发布检查。所有会进入线上 `main` 的提交必须使用同一提交的前端源码、云函数、迁移文件和依赖，按 **数据库迁移 → 云函数 → 网页 → 线上验证** 的顺序发布；缺少任一步骤时不得把该提交宣称为已发布，也不得推送至会自动发布网页的 `main`。不得为了赶发布跳过迁移状态核对。

实际执行顺序：先在本地完成构建、测试和提交，取得提交 SHA；核对线上数据库结构及 Supabase CLI 迁移历史，确认一致后应用该提交新增的迁移；从同一提交部署云函数；最后推送该提交以发布 GitHub Pages 网页；验证三部分后创建并推送**指向该提交**的附注 Git 标签。标签说明必须记录完整 SHA、已应用迁移编号、云函数和网页验证结果。若任何一步失败，停止后续发布，报告当前线上各部分的实际状态和恢复步骤；不得用标签掩盖部分发布。

`202609240001`、`202609240002`、`202609240003` 的线上结构已于 2026-09-26 核实，同日将这三条已有 SQL 对齐到 Supabase CLI 迁移历史。每次后续发布仍须重新查询本地与远端的迁移状态，确认一致后才能执行新增迁移；禁止盲目修复历史。数据库回退必须用新的修正迁移或已验证的备份恢复，不能把 Git 切换版本当作数据库回退。
